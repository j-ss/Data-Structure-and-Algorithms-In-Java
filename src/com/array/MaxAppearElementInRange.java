package com.array;

public class MaxAppearElementInRange {
    //one solution is use hash table and count the element
    //second solution if range size is small.<1000
    //then create an array of that size and replace value with 1 for range start
    //index and replace with -1 at index range end+1 now compute prefix sum that is solu.
}
